document.addEventListener('DOMContentLoaded', function() {
    const loginForm = document.getElementById('loginForm');
    const messageDiv = document.getElementById('message');

    loginForm.addEventListener('submit', function(event) {
        event.preventDefault(); // Prevent the default form submission

        const username = document.getElementById('username').value;
        const password = document.getElementById('password').value;

        // Basic validation (replace with actual authentication logic on a server)
        if (username === 'user' && password === 'password') {
            messageDiv.textContent = 'Login successful! Redirecting...';
            messageDiv.className = 'message success';
            // In a real application, you'd redirect to a dashboard or home page
            // window.location.href = '/dashboard.html';
        } else {
            messageDiv.textContent = 'Invalid username or password.';
            messageDiv.className = 'message error';
        }
    });
});body {
    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;
    position: relative; /* Needed for absolute positioning of content */
    min-height: 100vh; /* Ensure body takes full height */
}

.full-background-image {
    position: fixed; /* Keep it fixed in the viewport */
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover; /* Similar to background-size: cover */
    z-index: -1; /* Place it behind all other content */
}

.content-wrapper {
    position: relative; /* Allows content to be layered above the image */
    z-index: 1; /* Ensures content is above the image */
    /* Add styling for your content here, e.g., padding, background-color */
    background-color: rgba(255, 255, 255, 0.8);
    margin: 20px auto;
    padding: 20px;
    border-radius: 8px;
    max-width: 800px;
    box-shadow: 0 0 10px rgba(0,0,0,0.1);
}

h1 {
    text-align: center;
    color: #0056b3;
}body {
    background-image: url('path/to/your/image.jpg'); /* Replace with the actual path to your image */
    background-size: cover; /* Ensures the image covers the entire background */
    background-repeat: no-repeat; /* Prevents the image from repeating */
    background-attachment: fixed; /* Keeps the background image fixed when scrolling */
    background-position: center center; /* Centers the background image */
}

/* Optional: Add some basic styling for your content to show the background */
.content {
    margin: 50px;
    padding: 20px;
    background-color: rgba(255, 255, 255, 0.8); /* Semi-transparent white background for content */
    border-radius: 8px;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
}